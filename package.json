{
    "name": "@rejetto/kvstorage",
    "version": "0.3.3",
    "description": "Persistent key-value storage functionality with an API inspired by levelDB. All keys are kept in memory. Concurrency not supported.",
    "main": ".",
    "type": "commonjs",
    "scripts": {
        "build": "tsc",
        "test": "tsc && node test",
        "pub": "npm run test && npm pub"
    },
    "files": [
        "index.ts",
        "index.js",
        "index.js.map"
    ],
    "peerDependencies": {
        "typescript": ">=4.9.0"
    },
    "devDependencies": {
        "@tsconfig/node18": "^18.2.4",
        "@types/node": "^18.19.0"
    }
}
